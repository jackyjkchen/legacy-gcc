diff --git a/gcc/config/mips/mips-msa.md b/gcc/config/mips/mips-msa.md
index 929646d3b..14e36fea1 100644
--- a/gcc/config/mips/mips-msa.md
+++ b/gcc/config/mips/mips-msa.md
@@ -580,6 +580,24 @@
   operands[2] = reg;
 })
 
+(define_insn_and_split "neg<mode>2_insn"
+  [(set (match_operand:MSA 0 "register_operand" "=f")
+	(neg:MSA (match_operand:MSA 1 "register_operand" "f")))]
+  "ISA_HAS_MSA"
+{
+    return "#";
+}
+  "ISA_HAS_MSA"
+  [(const_int 0)]
+{
+  rtx reg = gen_reg_rtx (<MODE>mode);
+  emit_move_insn (reg, CONST0_RTX (<MODE>mode));
+  emit_insn(gen_sub<mode>3(operands[0], reg, operands[1]));
+  DONE;
+}
+  [(set_attr "type" "simd_logic")
+   (set_attr "mode" "<MODE>")])
+
 (define_expand "msa_ldi<mode>"
   [(match_operand:IMSA 0 "register_operand")
    (match_operand 1 "const_imm10_operand")]

diff --git a/Makefile.in b/Makefile.in
index f6897199..d668b132 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -30684,7 +30684,7 @@ maybe-clean-stage1-bfd:
 @if bfd-bootstrap
 maybe-configure-stage1-bfd: configure-stage1-bfd
 configure-stage1-bfd:
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f bfd/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -30708,7 +30708,7 @@ configure-stage1-bfd:
 
 maybe-all-stage1-bfd: all-stage1-bfd
 all-stage1-bfd: configure-stage1-bfd
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -30720,7 +30720,7 @@ maybe-clean-stage1-bfd: clean-stage1-bfd
 clean-stage1-bfd:
 	@[ -f bfd/Makefile ] || [ -f stage1-bfd/Makefile ] \
 	  || exit 0 ; \
-	[ -f bfd/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd bfd && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)"  clean
@@ -30738,7 +30738,7 @@ maybe-clean-stage1-opcodes:
 @if opcodes-bootstrap
 maybe-configure-stage1-opcodes: configure-stage1-opcodes
 configure-stage1-opcodes:
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f opcodes/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -30762,7 +30762,7 @@ configure-stage1-opcodes:
 
 maybe-all-stage1-opcodes: all-stage1-opcodes
 all-stage1-opcodes: configure-stage1-opcodes
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -30774,7 +30774,7 @@ maybe-clean-stage1-opcodes: clean-stage1-opcodes
 clean-stage1-opcodes:
 	@[ -f opcodes/Makefile ] || [ -f stage1-opcodes/Makefile ] \
 	  || exit 0 ; \
-	[ -f opcodes/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd opcodes && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)"  clean
@@ -30792,7 +30792,7 @@ maybe-clean-stage1-binutils:
 @if binutils-bootstrap
 maybe-configure-stage1-binutils: configure-stage1-binutils
 configure-stage1-binutils:
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f binutils/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -30816,7 +30816,7 @@ configure-stage1-binutils:
 
 maybe-all-stage1-binutils: all-stage1-binutils
 all-stage1-binutils: configure-stage1-binutils
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -30828,7 +30828,7 @@ maybe-clean-stage1-binutils: clean-stage1-binutils
 clean-stage1-binutils:
 	@[ -f binutils/Makefile ] || [ -f stage1-binutils/Makefile ] \
 	  || exit 0 ; \
-	[ -f binutils/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd binutils && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)"  clean
@@ -30846,7 +30846,7 @@ maybe-clean-stage1-gas:
 @if gas-bootstrap
 maybe-configure-stage1-gas: configure-stage1-gas
 configure-stage1-gas:
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f gas/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -30870,7 +30870,7 @@ configure-stage1-gas:
 
 maybe-all-stage1-gas: all-stage1-gas
 all-stage1-gas: configure-stage1-gas
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -30882,7 +30882,7 @@ maybe-clean-stage1-gas: clean-stage1-gas
 clean-stage1-gas:
 	@[ -f gas/Makefile ] || [ -f stage1-gas/Makefile ] \
 	  || exit 0 ; \
-	[ -f gas/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd gas && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)"  clean
@@ -30900,7 +30900,7 @@ maybe-clean-stage1-gcc:
 @if gcc-bootstrap
 maybe-configure-stage1-gcc: configure-stage1-gcc
 configure-stage1-gcc:
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f gcc/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -30924,7 +30924,7 @@ configure-stage1-gcc:
 
 maybe-all-stage1-gcc: all-stage1-gcc
 all-stage1-gcc: configure-stage1-gcc
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -30936,7 +30936,7 @@ maybe-clean-stage1-gcc: clean-stage1-gcc
 clean-stage1-gcc:
 	@[ -f gcc/Makefile ] || [ -f stage1-gcc/Makefile ] \
 	  || exit 0 ; \
-	[ -f gcc/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd gcc && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)" $(EXTRA_GCC_FLAGS) clean
@@ -30953,8 +30953,8 @@ maybe-clean-stage1-intl:
 
 @if intl-bootstrap
 maybe-configure-stage1-intl: configure-stage1-intl
-configure-stage1-intl:
-	@$(MAKE) stage1-start
+configure-stage1-intl: maybe-configure-stage1-libiberty
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f intl/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -30978,7 +30978,7 @@ configure-stage1-intl:
 
 maybe-all-stage1-intl: all-stage1-intl
 all-stage1-intl: configure-stage1-intl
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -30990,7 +30990,7 @@ maybe-clean-stage1-intl: clean-stage1-intl
 clean-stage1-intl:
 	@[ -f intl/Makefile ] || [ -f stage1-intl/Makefile ] \
 	  || exit 0 ; \
-	[ -f intl/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd intl && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)"  clean
@@ -31008,7 +31008,7 @@ maybe-clean-stage1-ld:
 @if ld-bootstrap
 maybe-configure-stage1-ld: configure-stage1-ld
 configure-stage1-ld:
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f ld/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31032,7 +31032,7 @@ configure-stage1-ld:
 
 maybe-all-stage1-ld: all-stage1-ld
 all-stage1-ld: configure-stage1-ld
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -31044,7 +31044,7 @@ maybe-clean-stage1-ld: clean-stage1-ld
 clean-stage1-ld:
 	@[ -f ld/Makefile ] || [ -f stage1-ld/Makefile ] \
 	  || exit 0 ; \
-	[ -f ld/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd ld && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)"  clean
@@ -31062,7 +31062,7 @@ maybe-clean-stage1-libcpp:
 @if libcpp-bootstrap
 maybe-configure-stage1-libcpp: configure-stage1-libcpp
 configure-stage1-libcpp:
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f libcpp/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31086,7 +31086,7 @@ configure-stage1-libcpp:
 
 maybe-all-stage1-libcpp: all-stage1-libcpp
 all-stage1-libcpp: configure-stage1-libcpp
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -31098,7 +31098,7 @@ maybe-clean-stage1-libcpp: clean-stage1-libcpp
 clean-stage1-libcpp:
 	@[ -f libcpp/Makefile ] || [ -f stage1-libcpp/Makefile ] \
 	  || exit 0 ; \
-	[ -f libcpp/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd libcpp && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)"  clean
@@ -31116,7 +31116,7 @@ maybe-clean-stage1-libiberty:
 @if libiberty-bootstrap
 maybe-configure-stage1-libiberty: configure-stage1-libiberty
 configure-stage1-libiberty:
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f libiberty/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31140,7 +31140,7 @@ configure-stage1-libiberty:
 
 maybe-all-stage1-libiberty: all-stage1-libiberty
 all-stage1-libiberty: configure-stage1-libiberty
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -31152,7 +31152,7 @@ maybe-clean-stage1-libiberty: clean-stage1-libiberty
 clean-stage1-libiberty:
 	@[ -f libiberty/Makefile ] || [ -f stage1-libiberty/Makefile ] \
 	  || exit 0 ; \
-	[ -f libiberty/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd libiberty && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)"  clean
@@ -31170,7 +31170,7 @@ maybe-clean-stage1-zlib:
 @if zlib-bootstrap
 maybe-configure-stage1-zlib: configure-stage1-zlib
 configure-stage1-zlib:
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@[ -f zlib/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31194,7 +31194,7 @@ configure-stage1-zlib:
 
 maybe-all-stage1-zlib: all-stage1-zlib
 all-stage1-zlib: configure-stage1-zlib
-	@$(MAKE) stage1-start
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(HOST_EXPORTS)  \
@@ -31206,7 +31206,7 @@ maybe-clean-stage1-zlib: clean-stage1-zlib
 clean-stage1-zlib:
 	@[ -f zlib/Makefile ] || [ -f stage1-zlib/Makefile ] \
 	  || exit 0 ; \
-	[ -f zlib/Makefile ] || $(MAKE) stage1-start ; \
+	@[ `cat stage_current` = stage1 ] || $(MAKE) stage1-start
 	cd zlib && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		CFLAGS="$(STAGE1_CFLAGS)"  clean
@@ -31381,7 +31381,7 @@ maybe-clean-stage2-bfd:
 @if bfd-bootstrap
 maybe-configure-stage2-bfd: configure-stage2-bfd
 configure-stage2-bfd:
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f bfd/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31405,7 +31405,7 @@ configure-stage2-bfd:
 
 maybe-all-stage2-bfd: all-stage2-bfd
 all-stage2-bfd: configure-stage2-bfd
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31418,7 +31418,7 @@ maybe-clean-stage2-bfd: clean-stage2-bfd
 clean-stage2-bfd:
 	@[ -f bfd/Makefile ] || [ -f stage2-bfd/Makefile ] \
 	  || exit 0 ; \
-	[ -f bfd/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd bfd && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -31437,7 +31437,7 @@ maybe-clean-stage2-opcodes:
 @if opcodes-bootstrap
 maybe-configure-stage2-opcodes: configure-stage2-opcodes
 configure-stage2-opcodes:
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f opcodes/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31461,7 +31461,7 @@ configure-stage2-opcodes:
 
 maybe-all-stage2-opcodes: all-stage2-opcodes
 all-stage2-opcodes: configure-stage2-opcodes
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31474,7 +31474,7 @@ maybe-clean-stage2-opcodes: clean-stage2-opcodes
 clean-stage2-opcodes:
 	@[ -f opcodes/Makefile ] || [ -f stage2-opcodes/Makefile ] \
 	  || exit 0 ; \
-	[ -f opcodes/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd opcodes && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -31493,7 +31493,7 @@ maybe-clean-stage2-binutils:
 @if binutils-bootstrap
 maybe-configure-stage2-binutils: configure-stage2-binutils
 configure-stage2-binutils:
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f binutils/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31517,7 +31517,7 @@ configure-stage2-binutils:
 
 maybe-all-stage2-binutils: all-stage2-binutils
 all-stage2-binutils: configure-stage2-binutils
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31530,7 +31530,7 @@ maybe-clean-stage2-binutils: clean-stage2-binutils
 clean-stage2-binutils:
 	@[ -f binutils/Makefile ] || [ -f stage2-binutils/Makefile ] \
 	  || exit 0 ; \
-	[ -f binutils/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd binutils && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -31549,7 +31549,7 @@ maybe-clean-stage2-gas:
 @if gas-bootstrap
 maybe-configure-stage2-gas: configure-stage2-gas
 configure-stage2-gas:
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f gas/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31573,7 +31573,7 @@ configure-stage2-gas:
 
 maybe-all-stage2-gas: all-stage2-gas
 all-stage2-gas: configure-stage2-gas
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31586,7 +31586,7 @@ maybe-clean-stage2-gas: clean-stage2-gas
 clean-stage2-gas:
 	@[ -f gas/Makefile ] || [ -f stage2-gas/Makefile ] \
 	  || exit 0 ; \
-	[ -f gas/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd gas && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -31605,7 +31605,7 @@ maybe-clean-stage2-gcc:
 @if gcc-bootstrap
 maybe-configure-stage2-gcc: configure-stage2-gcc
 configure-stage2-gcc:
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f gcc/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31629,7 +31629,7 @@ configure-stage2-gcc:
 
 maybe-all-stage2-gcc: all-stage2-gcc
 all-stage2-gcc: configure-stage2-gcc
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31642,7 +31642,7 @@ maybe-clean-stage2-gcc: clean-stage2-gcc
 clean-stage2-gcc:
 	@[ -f gcc/Makefile ] || [ -f stage2-gcc/Makefile ] \
 	  || exit 0 ; \
-	[ -f gcc/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd gcc && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -31660,8 +31660,8 @@ maybe-clean-stage2-intl:
 
 @if intl-bootstrap
 maybe-configure-stage2-intl: configure-stage2-intl
-configure-stage2-intl:
-	@$(MAKE) stage2-start
+configure-stage2-intl: maybe-configure-stage2-libiberty
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f intl/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31685,7 +31685,7 @@ configure-stage2-intl:
 
 maybe-all-stage2-intl: all-stage2-intl
 all-stage2-intl: configure-stage2-intl
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31698,7 +31698,7 @@ maybe-clean-stage2-intl: clean-stage2-intl
 clean-stage2-intl:
 	@[ -f intl/Makefile ] || [ -f stage2-intl/Makefile ] \
 	  || exit 0 ; \
-	[ -f intl/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd intl && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -31717,7 +31717,7 @@ maybe-clean-stage2-ld:
 @if ld-bootstrap
 maybe-configure-stage2-ld: configure-stage2-ld
 configure-stage2-ld:
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f ld/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31741,7 +31741,7 @@ configure-stage2-ld:
 
 maybe-all-stage2-ld: all-stage2-ld
 all-stage2-ld: configure-stage2-ld
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31754,7 +31754,7 @@ maybe-clean-stage2-ld: clean-stage2-ld
 clean-stage2-ld:
 	@[ -f ld/Makefile ] || [ -f stage2-ld/Makefile ] \
 	  || exit 0 ; \
-	[ -f ld/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd ld && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -31773,7 +31773,7 @@ maybe-clean-stage2-libcpp:
 @if libcpp-bootstrap
 maybe-configure-stage2-libcpp: configure-stage2-libcpp
 configure-stage2-libcpp:
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f libcpp/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31797,7 +31797,7 @@ configure-stage2-libcpp:
 
 maybe-all-stage2-libcpp: all-stage2-libcpp
 all-stage2-libcpp: configure-stage2-libcpp
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31810,7 +31810,7 @@ maybe-clean-stage2-libcpp: clean-stage2-libcpp
 clean-stage2-libcpp:
 	@[ -f libcpp/Makefile ] || [ -f stage2-libcpp/Makefile ] \
 	  || exit 0 ; \
-	[ -f libcpp/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd libcpp && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -31829,7 +31829,7 @@ maybe-clean-stage2-libiberty:
 @if libiberty-bootstrap
 maybe-configure-stage2-libiberty: configure-stage2-libiberty
 configure-stage2-libiberty:
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f libiberty/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31853,7 +31853,7 @@ configure-stage2-libiberty:
 
 maybe-all-stage2-libiberty: all-stage2-libiberty
 all-stage2-libiberty: configure-stage2-libiberty
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31866,7 +31866,7 @@ maybe-clean-stage2-libiberty: clean-stage2-libiberty
 clean-stage2-libiberty:
 	@[ -f libiberty/Makefile ] || [ -f stage2-libiberty/Makefile ] \
 	  || exit 0 ; \
-	[ -f libiberty/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd libiberty && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -31885,7 +31885,7 @@ maybe-clean-stage2-zlib:
 @if zlib-bootstrap
 maybe-configure-stage2-zlib: configure-stage2-zlib
 configure-stage2-zlib:
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@[ -f zlib/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -31909,7 +31909,7 @@ configure-stage2-zlib:
 
 maybe-all-stage2-zlib: all-stage2-zlib
 all-stage2-zlib: configure-stage2-zlib
-	@$(MAKE) stage2-start
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -31922,7 +31922,7 @@ maybe-clean-stage2-zlib: clean-stage2-zlib
 clean-stage2-zlib:
 	@[ -f zlib/Makefile ] || [ -f stage2-zlib/Makefile ] \
 	  || exit 0 ; \
-	[ -f zlib/Makefile ] || $(MAKE) stage2-start ; \
+	@[ `cat stage_current` = stage2 ] || $(MAKE) stage2-start
 	cd zlib && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32010,47 +32010,6 @@ stage3-start::
 @endif zlib
 
 stage3-end::
-	@rm -f stage_current
-@if bfd
-	@set bfd stage3-bfd ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-bfd stage2-bfd ; @UNDO_LINK_TO_DIR@ 
-@endif bfd
-@if opcodes
-	@set opcodes stage3-opcodes ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-opcodes stage2-opcodes ; @UNDO_LINK_TO_DIR@ 
-@endif opcodes
-@if binutils
-	@set binutils stage3-binutils ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-binutils stage2-binutils ; @UNDO_LINK_TO_DIR@ 
-@endif binutils
-@if gas
-	@set gas stage3-gas ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-gas stage2-gas ; @UNDO_LINK_TO_DIR@ 
-@endif gas
-@if gcc
-	@set gcc stage3-gcc ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-gcc stage2-gcc ; @UNDO_LINK_TO_DIR@ 
-@endif gcc
-@if intl
-	@set intl stage3-intl ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-intl stage2-intl ; @UNDO_LINK_TO_DIR@ 
-@endif intl
-@if ld
-	@set ld stage3-ld ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-ld stage2-ld ; @UNDO_LINK_TO_DIR@ 
-@endif ld
-@if libcpp
-	@set libcpp stage3-libcpp ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-libcpp stage2-libcpp ; @UNDO_LINK_TO_DIR@ 
-@endif libcpp
-@if libiberty
-	@set libiberty stage3-libiberty ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-libiberty stage2-libiberty ; @UNDO_LINK_TO_DIR@ 
-@endif libiberty
-@if zlib
-	@set zlib stage3-zlib ; @UNDO_LINK_TO_DIR@  ; \
-	set prev-zlib stage2-zlib ; @UNDO_LINK_TO_DIR@ 
-@endif zlib
 
 # Bubble a bugfix through all the stages up to stage 3.  They
 # are remade, but not reconfigured.  The next stage (if any) will not
@@ -32102,7 +32061,7 @@ maybe-clean-stage3-bfd:
 @if bfd-bootstrap
 maybe-configure-stage3-bfd: configure-stage3-bfd
 configure-stage3-bfd:
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f bfd/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32126,7 +32085,7 @@ configure-stage3-bfd:
 
 maybe-all-stage3-bfd: all-stage3-bfd
 all-stage3-bfd: configure-stage3-bfd
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32139,7 +32098,7 @@ maybe-clean-stage3-bfd: clean-stage3-bfd
 clean-stage3-bfd:
 	@[ -f bfd/Makefile ] || [ -f stage3-bfd/Makefile ] \
 	  || exit 0 ; \
-	[ -f bfd/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd bfd && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32158,7 +32117,7 @@ maybe-clean-stage3-opcodes:
 @if opcodes-bootstrap
 maybe-configure-stage3-opcodes: configure-stage3-opcodes
 configure-stage3-opcodes:
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f opcodes/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32182,7 +32141,7 @@ configure-stage3-opcodes:
 
 maybe-all-stage3-opcodes: all-stage3-opcodes
 all-stage3-opcodes: configure-stage3-opcodes
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32195,7 +32154,7 @@ maybe-clean-stage3-opcodes: clean-stage3-opcodes
 clean-stage3-opcodes:
 	@[ -f opcodes/Makefile ] || [ -f stage3-opcodes/Makefile ] \
 	  || exit 0 ; \
-	[ -f opcodes/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd opcodes && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32214,7 +32173,7 @@ maybe-clean-stage3-binutils:
 @if binutils-bootstrap
 maybe-configure-stage3-binutils: configure-stage3-binutils
 configure-stage3-binutils:
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f binutils/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32238,7 +32197,7 @@ configure-stage3-binutils:
 
 maybe-all-stage3-binutils: all-stage3-binutils
 all-stage3-binutils: configure-stage3-binutils
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32251,7 +32210,7 @@ maybe-clean-stage3-binutils: clean-stage3-binutils
 clean-stage3-binutils:
 	@[ -f binutils/Makefile ] || [ -f stage3-binutils/Makefile ] \
 	  || exit 0 ; \
-	[ -f binutils/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd binutils && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32270,7 +32229,7 @@ maybe-clean-stage3-gas:
 @if gas-bootstrap
 maybe-configure-stage3-gas: configure-stage3-gas
 configure-stage3-gas:
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f gas/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32294,7 +32253,7 @@ configure-stage3-gas:
 
 maybe-all-stage3-gas: all-stage3-gas
 all-stage3-gas: configure-stage3-gas
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32307,7 +32266,7 @@ maybe-clean-stage3-gas: clean-stage3-gas
 clean-stage3-gas:
 	@[ -f gas/Makefile ] || [ -f stage3-gas/Makefile ] \
 	  || exit 0 ; \
-	[ -f gas/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd gas && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32326,7 +32285,7 @@ maybe-clean-stage3-gcc:
 @if gcc-bootstrap
 maybe-configure-stage3-gcc: configure-stage3-gcc
 configure-stage3-gcc:
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f gcc/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32350,7 +32309,7 @@ configure-stage3-gcc:
 
 maybe-all-stage3-gcc: all-stage3-gcc
 all-stage3-gcc: configure-stage3-gcc
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32363,7 +32322,7 @@ maybe-clean-stage3-gcc: clean-stage3-gcc
 clean-stage3-gcc:
 	@[ -f gcc/Makefile ] || [ -f stage3-gcc/Makefile ] \
 	  || exit 0 ; \
-	[ -f gcc/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd gcc && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32381,8 +32340,8 @@ maybe-clean-stage3-intl:
 
 @if intl-bootstrap
 maybe-configure-stage3-intl: configure-stage3-intl
-configure-stage3-intl:
-	@$(MAKE) stage3-start
+configure-stage3-intl: maybe-configure-stage3-libiberty
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f intl/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32406,7 +32365,7 @@ configure-stage3-intl:
 
 maybe-all-stage3-intl: all-stage3-intl
 all-stage3-intl: configure-stage3-intl
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32419,7 +32378,7 @@ maybe-clean-stage3-intl: clean-stage3-intl
 clean-stage3-intl:
 	@[ -f intl/Makefile ] || [ -f stage3-intl/Makefile ] \
 	  || exit 0 ; \
-	[ -f intl/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd intl && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32438,7 +32397,7 @@ maybe-clean-stage3-ld:
 @if ld-bootstrap
 maybe-configure-stage3-ld: configure-stage3-ld
 configure-stage3-ld:
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f ld/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32462,7 +32421,7 @@ configure-stage3-ld:
 
 maybe-all-stage3-ld: all-stage3-ld
 all-stage3-ld: configure-stage3-ld
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32475,7 +32434,7 @@ maybe-clean-stage3-ld: clean-stage3-ld
 clean-stage3-ld:
 	@[ -f ld/Makefile ] || [ -f stage3-ld/Makefile ] \
 	  || exit 0 ; \
-	[ -f ld/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd ld && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32494,7 +32453,7 @@ maybe-clean-stage3-libcpp:
 @if libcpp-bootstrap
 maybe-configure-stage3-libcpp: configure-stage3-libcpp
 configure-stage3-libcpp:
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f libcpp/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32518,7 +32477,7 @@ configure-stage3-libcpp:
 
 maybe-all-stage3-libcpp: all-stage3-libcpp
 all-stage3-libcpp: configure-stage3-libcpp
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32531,7 +32490,7 @@ maybe-clean-stage3-libcpp: clean-stage3-libcpp
 clean-stage3-libcpp:
 	@[ -f libcpp/Makefile ] || [ -f stage3-libcpp/Makefile ] \
 	  || exit 0 ; \
-	[ -f libcpp/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd libcpp && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32550,7 +32509,7 @@ maybe-clean-stage3-libiberty:
 @if libiberty-bootstrap
 maybe-configure-stage3-libiberty: configure-stage3-libiberty
 configure-stage3-libiberty:
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f libiberty/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32574,7 +32533,7 @@ configure-stage3-libiberty:
 
 maybe-all-stage3-libiberty: all-stage3-libiberty
 all-stage3-libiberty: configure-stage3-libiberty
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32587,7 +32546,7 @@ maybe-clean-stage3-libiberty: clean-stage3-libiberty
 clean-stage3-libiberty:
 	@[ -f libiberty/Makefile ] || [ -f stage3-libiberty/Makefile ] \
 	  || exit 0 ; \
-	[ -f libiberty/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd libiberty && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32606,7 +32565,7 @@ maybe-clean-stage3-zlib:
 @if zlib-bootstrap
 maybe-configure-stage3-zlib: configure-stage3-zlib
 configure-stage3-zlib:
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@[ -f zlib/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32630,7 +32589,7 @@ configure-stage3-zlib:
 
 maybe-all-stage3-zlib: all-stage3-zlib
 all-stage3-zlib: configure-stage3-zlib
-	@$(MAKE) stage3-start
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32643,7 +32602,7 @@ maybe-clean-stage3-zlib: clean-stage3-zlib
 clean-stage3-zlib:
 	@[ -f zlib/Makefile ] || [ -f stage3-zlib/Makefile ] \
 	  || exit 0 ; \
-	[ -f zlib/Makefile ] || $(MAKE) stage3-start ; \
+	@[ `cat stage_current` = stage3 ] || $(MAKE) stage3-start
 	cd zlib && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32662,7 +32621,7 @@ compare:
 	  exit 0 ; \
 	fi; \
 	[ -f stage_current ] && $(MAKE) `cat stage_current`-end || : ; \
-	@r=`${PWD_COMMAND}`; export r; \
+	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s; \
 	rm -f .bad_compare ; \
 	cd stage3-gcc; \
@@ -32853,7 +32812,7 @@ maybe-clean-stage4-bfd:
 @if bfd-bootstrap
 maybe-configure-stage4-bfd: configure-stage4-bfd
 configure-stage4-bfd:
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f bfd/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32877,7 +32836,7 @@ configure-stage4-bfd:
 
 maybe-all-stage4-bfd: all-stage4-bfd
 all-stage4-bfd: configure-stage4-bfd
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32890,7 +32849,7 @@ maybe-clean-stage4-bfd: clean-stage4-bfd
 clean-stage4-bfd:
 	@[ -f bfd/Makefile ] || [ -f stage4-bfd/Makefile ] \
 	  || exit 0 ; \
-	[ -f bfd/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd bfd && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32909,7 +32868,7 @@ maybe-clean-stage4-opcodes:
 @if opcodes-bootstrap
 maybe-configure-stage4-opcodes: configure-stage4-opcodes
 configure-stage4-opcodes:
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f opcodes/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32933,7 +32892,7 @@ configure-stage4-opcodes:
 
 maybe-all-stage4-opcodes: all-stage4-opcodes
 all-stage4-opcodes: configure-stage4-opcodes
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -32946,7 +32905,7 @@ maybe-clean-stage4-opcodes: clean-stage4-opcodes
 clean-stage4-opcodes:
 	@[ -f opcodes/Makefile ] || [ -f stage4-opcodes/Makefile ] \
 	  || exit 0 ; \
-	[ -f opcodes/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd opcodes && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -32965,7 +32924,7 @@ maybe-clean-stage4-binutils:
 @if binutils-bootstrap
 maybe-configure-stage4-binutils: configure-stage4-binutils
 configure-stage4-binutils:
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f binutils/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -32989,7 +32948,7 @@ configure-stage4-binutils:
 
 maybe-all-stage4-binutils: all-stage4-binutils
 all-stage4-binutils: configure-stage4-binutils
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -33002,7 +32961,7 @@ maybe-clean-stage4-binutils: clean-stage4-binutils
 clean-stage4-binutils:
 	@[ -f binutils/Makefile ] || [ -f stage4-binutils/Makefile ] \
 	  || exit 0 ; \
-	[ -f binutils/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd binutils && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -33021,7 +32980,7 @@ maybe-clean-stage4-gas:
 @if gas-bootstrap
 maybe-configure-stage4-gas: configure-stage4-gas
 configure-stage4-gas:
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f gas/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -33045,7 +33004,7 @@ configure-stage4-gas:
 
 maybe-all-stage4-gas: all-stage4-gas
 all-stage4-gas: configure-stage4-gas
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -33058,7 +33017,7 @@ maybe-clean-stage4-gas: clean-stage4-gas
 clean-stage4-gas:
 	@[ -f gas/Makefile ] || [ -f stage4-gas/Makefile ] \
 	  || exit 0 ; \
-	[ -f gas/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd gas && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -33077,7 +33036,7 @@ maybe-clean-stage4-gcc:
 @if gcc-bootstrap
 maybe-configure-stage4-gcc: configure-stage4-gcc
 configure-stage4-gcc:
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f gcc/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -33101,7 +33060,7 @@ configure-stage4-gcc:
 
 maybe-all-stage4-gcc: all-stage4-gcc
 all-stage4-gcc: configure-stage4-gcc
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -33114,7 +33073,7 @@ maybe-clean-stage4-gcc: clean-stage4-gcc
 clean-stage4-gcc:
 	@[ -f gcc/Makefile ] || [ -f stage4-gcc/Makefile ] \
 	  || exit 0 ; \
-	[ -f gcc/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd gcc && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -33132,8 +33091,8 @@ maybe-clean-stage4-intl:
 
 @if intl-bootstrap
 maybe-configure-stage4-intl: configure-stage4-intl
-configure-stage4-intl:
-	@$(MAKE) stage4-start
+configure-stage4-intl: maybe-configure-stage4-libiberty
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f intl/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -33157,7 +33116,7 @@ configure-stage4-intl:
 
 maybe-all-stage4-intl: all-stage4-intl
 all-stage4-intl: configure-stage4-intl
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -33170,7 +33129,7 @@ maybe-clean-stage4-intl: clean-stage4-intl
 clean-stage4-intl:
 	@[ -f intl/Makefile ] || [ -f stage4-intl/Makefile ] \
 	  || exit 0 ; \
-	[ -f intl/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd intl && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -33189,7 +33148,7 @@ maybe-clean-stage4-ld:
 @if ld-bootstrap
 maybe-configure-stage4-ld: configure-stage4-ld
 configure-stage4-ld:
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f ld/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -33213,7 +33172,7 @@ configure-stage4-ld:
 
 maybe-all-stage4-ld: all-stage4-ld
 all-stage4-ld: configure-stage4-ld
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -33226,7 +33185,7 @@ maybe-clean-stage4-ld: clean-stage4-ld
 clean-stage4-ld:
 	@[ -f ld/Makefile ] || [ -f stage4-ld/Makefile ] \
 	  || exit 0 ; \
-	[ -f ld/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd ld && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -33245,7 +33204,7 @@ maybe-clean-stage4-libcpp:
 @if libcpp-bootstrap
 maybe-configure-stage4-libcpp: configure-stage4-libcpp
 configure-stage4-libcpp:
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f libcpp/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -33269,7 +33228,7 @@ configure-stage4-libcpp:
 
 maybe-all-stage4-libcpp: all-stage4-libcpp
 all-stage4-libcpp: configure-stage4-libcpp
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -33282,7 +33241,7 @@ maybe-clean-stage4-libcpp: clean-stage4-libcpp
 clean-stage4-libcpp:
 	@[ -f libcpp/Makefile ] || [ -f stage4-libcpp/Makefile ] \
 	  || exit 0 ; \
-	[ -f libcpp/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd libcpp && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -33301,7 +33260,7 @@ maybe-clean-stage4-libiberty:
 @if libiberty-bootstrap
 maybe-configure-stage4-libiberty: configure-stage4-libiberty
 configure-stage4-libiberty:
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f libiberty/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -33325,7 +33284,7 @@ configure-stage4-libiberty:
 
 maybe-all-stage4-libiberty: all-stage4-libiberty
 all-stage4-libiberty: configure-stage4-libiberty
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -33338,7 +33297,7 @@ maybe-clean-stage4-libiberty: clean-stage4-libiberty
 clean-stage4-libiberty:
 	@[ -f libiberty/Makefile ] || [ -f stage4-libiberty/Makefile ] \
 	  || exit 0 ; \
-	[ -f libiberty/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd libiberty && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
@@ -33357,7 +33316,7 @@ maybe-clean-stage4-zlib:
 @if zlib-bootstrap
 maybe-configure-stage4-zlib: configure-stage4-zlib
 configure-stage4-zlib:
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@[ -f zlib/Makefile ] && exit 0 || : ; \
 	r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
@@ -33381,7 +33340,7 @@ configure-stage4-zlib:
 
 maybe-all-stage4-zlib: all-stage4-zlib
 all-stage4-zlib: configure-stage4-zlib
-	@$(MAKE) stage4-start
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start
 	@r=`${PWD_COMMAND}`; export r; \
 	s=`cd $(srcdir); ${PWD_COMMAND}`; export s;  \
 	$(STAGE_HOST_EXPORTS)  \
@@ -33394,7 +33353,7 @@ maybe-clean-stage4-zlib: clean-stage4-zlib
 clean-stage4-zlib:
 	@[ -f zlib/Makefile ] || [ -f stage4-zlib/Makefile ] \
 	  || exit 0 ; \
-	[ -f zlib/Makefile ] || $(MAKE) stage4-start ; \
+	@[ `cat stage_current` = stage4 ] || $(MAKE) stage4-start \
 	cd zlib && \
 	$(MAKE) $(FLAGS_TO_PASS)  \
 		$(POSTSTAGE1_FLAGS_TO_PASS)  \
